#ifndef INSERTION_SHELL_H
#define INSERTION_SHELL_SORT_H
#include "aux.h"

void insertionSort(int v[], int l, int r, sortperf_t *s);
void shellSort(int *A, int n, sortperf_t *s);

#endif // INSERTION_SHELL_SORT_H 